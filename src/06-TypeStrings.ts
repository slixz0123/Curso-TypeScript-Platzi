(()=>{
  let ProductTitle = 'My AMazing product ';
  console.log('ProductTitle', ProductTitle);

  const summary = `
     tittle : ${ProductTitle}
     producname 2  :  ${myProductName2}
  ` ;  // backtic nos da el veneficio de contatenar multiple lineas con variables de las clases

})
